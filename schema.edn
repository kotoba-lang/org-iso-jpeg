;; schema.edn — Datomic/Datascript 互換スキーマ定義（自動生成 by edn-datomize.bb）
;; :db/ident 属性定義のリスト。Datomic 固有キー(:db.install/_attribute 等)は使わない。
;; 手編集禁止 — 再生成すると上書きされる。
;; 注: kotoba/schema.edn（手書きの正本スキーマ）とは別物 — こちらは
;; edn-datomize.bb が変換した各種 .edn ファイル（actor/cell/lex 等の
;; メタデータ）専用の付随スキーマ。

[#:db{:ident :resources.jpeg.fixtures.sample-rgb/h, :valueType :db.type/long, :cardinality :db.cardinality/one} #:db{:ident :resources.jpeg.fixtures.sample-rgb/rgb, :valueType :db.type/long, :cardinality :db.cardinality/many} #:db{:ident :resources.jpeg.fixtures.sample-rgb/w, :valueType :db.type/long, :cardinality :db.cardinality/one}]
